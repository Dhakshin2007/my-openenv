You are an expert Python and FastAPI developer. Below are multiple files from a GitHub repository called `my-openenv` — a SQL debugging reinforcement learning environment built for the Meta x PyTorch Hackathon on Scaler. The project runs on Hugging Face Spaces (port 7860) and exposes an OpenEnv REST API.

I need you to fix ALL of the following confirmed bugs across the files. Do NOT change any logic that is not related to the bugs listed. Return the full corrected file content for each file that needs changes.

═══════════════════════════════════════════════════════════════
BUG #1 — inference.py: Missing structured log format (CRITICAL)
═══════════════════════════════════════════════════════════════
The hackathon evaluator expects stdout to have structured log lines in this exact format:
  [START] task_id=<task_id>
  [STEP] step=<n> action=<action_type> detail=<detail>
  [REWARD] value=<value> reason=<reason>
  [END] task_id=<task_id> score=<score>
  [SUMMARY] avg_score=<avg> elapsed=<seconds>s

Replace ALL the current print() statements in inference.py with the above structured log format.
- [START] → printed once at the beginning of each episode
- [STEP] → printed for every action taken
- [REWARD] → printed after every step result
- [END] → printed once at the end of each episode with final score
- [SUMMARY] → printed once at the very end after all episodes
Keep all other logic (LLM calls, env_post, env_get, run_episode, main) identical.

═══════════════════════════════════════════════════════════════
BUG #2 — main.py: Leaderboard task_id logic is inverted
═══════════════════════════════════════════════════════════════
In the /step/{session_id} endpoint, the following line has the condition backwards:
  task_id = env.state().task_id if not result.done else result.observation.task_id

When result.done is True, env.state() works fine (env still in local scope), but the intent
was to avoid calling env.state() after the session is popped. Fix this to simply use:
  task_id = result.observation.task_id

This should be done BEFORE the _sessions.pop() call. Reorder the logic so the leaderboard
entry is built and appended before the session is removed from _sessions.

═══════════════════════════════════════════════════════════════
BUG #3 — environment.py: _trigger_chaos uses hardcoded INSERT schema
═══════════════════════════════════════════════════════════════
This line will crash with OperationalError if the products table has a different column count or order:
  cur.execute("INSERT INTO products VALUES (999, 'Chaos Item', 'Electronics', -99.99, 13)")

Fix: Query PRAGMA table_info(products) first to get the actual column names and count,
then build a dynamic INSERT using only the columns that exist. If columns like
'name', 'category', 'price', 'stock' are present, insert only those with named columns.
Wrap the entire thing in a try/except so any failure is silently ignored.

═══════════════════════════════════════════════════════════════
BUG #4 — environment.py: _execute splits SQL on ";" breaking CTEs and multi-statement queries
═══════════════════════════════════════════════════════════════
The current implementation:
  for stmt in sql.split(";"):
This breaks queries that contain semicolons inside CTEs, subqueries, or multi-statement blocks.

Fix: Instead of splitting on ";", use cursor.executescript() only for DDL/DML setup,
and for regular agent queries use a single cursor.execute(sql.strip()) call.
If the SQL contains multiple statements (detected by checking for multiple non-empty
semicolon-separated parts), execute each one individually but handle CTE patterns
(WITH ... AS (...)) as a single atomic statement — do NOT split them.
The safe approach: try executing the entire sql string as-is first. Only if it fails
due to "multiple statements" error, fall back to splitting.

═══════════════════════════════════════════════════════════════
BUG #5 — Dockerfile: Single uvicorn worker blocks under Gradio mount
═══════════════════════════════════════════════════════════════
The CMD line:
  CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7860", "--workers", "1"]

With gr.mount_gradio_app(), a single worker can cause /reset to time out when Gradio
is handling requests. Fix: switch to using the uvicorn loop with a higher timeout and
add --timeout-keep-alive 75. Keep workers=1 (HF Spaces requirement for shared memory sessions),
but add --loop asyncio explicitly and --timeout-graceful-shutdown 10.

═══════════════════════════════════════════════════════════════
FILES TO FIX — here are the full current file contents:
═══════════════════════════════════════════════════════════════

──────────────────────────────────────────────
FILE: inference.py
──────────────────────────────────────────────
"""
inference.py — Baseline agent for SQL Debug Environment.

Usage:
  export API_BASE_URL=https://<your-hf-space>.hf.space
  export MODEL_NAME=meta-llama/Llama-3.1-70B-Instruct
  export HF_TOKEN=hf_...
  python inference.py

The agent:
  1. Calls POST /reset to start an episode.
  2. Uses the LLM to decide actions (examine_schema → run_query → submit_solution).
  3. Collects the grader score from the step result.
  4. Repeats for all 3 tasks and prints a summary.

Environment variables:
  API_BASE_URL   URL of the deployed OpenEnv server (default: http://localhost:7860)
  MODEL_NAME     LLM model identifier (default: gpt-4o-mini)
  HF_TOKEN       Hugging Face / OpenAI API key
  LLM_BASE_URL   Base URL for the OpenAI-compatible LLM API
                 (default: https://api.openai.com/v1)
"""

import json
import os
import sys
import time

import requests
from openai import OpenAI

# ── Configuration ────────────────────────────────────────────────────────────
ENV_BASE_URL: str = os.getenv("API_BASE_URL", "http://localhost:7860").rstrip("/")
MODEL_NAME: str   = os.getenv("MODEL_NAME", "gpt-4o-mini")
HF_TOKEN: str     = os.getenv("HF_TOKEN", os.getenv("OPENAI_API_KEY", ""))
LLM_BASE_URL: str = os.getenv("LLM_BASE_URL", "https://api.openai.com/v1")
MAX_EPISODE_STEPS: int = 20

# ── LLM client ───────────────────────────────────────────────────────────────
llm = OpenAI(base_url=LLM_BASE_URL, api_key=HF_TOKEN)

SYSTEM_PROMPT = """You are an expert SQL agent operating inside an interactive database environment.

AVAILABLE ACTIONS (respond with EXACTLY ONE JSON object, no markdown, no extra text):

1. Examine a table schema:
   {"action_type": "examine_schema", "table_name": "<name>"}

2. Run a SQL query to explore the data:
   {"action_type": "run_query", "sql": "<SELECT ...>"}

3. Submit your final answer:
   {"action_type": "submit_solution", "sql": "<corrected/written SQL>"}

STRATEGY:
- First examine ALL available tables to understand the schema.
- Run test queries to validate your understanding.
- Once confident, submit your solution.
- Be efficient: aim to solve tasks in ≤10 steps.
- Think carefully about the task requirements before submitting.
"""


def call_llm(messages: list) -> str:
    response = llm.chat.completions.create(
        model=MODEL_NAME,
        messages=[{"role": "system", "content": SYSTEM_PROMPT}] + messages,
        temperature=0.0,
        max_tokens=512,
    )
    if not response.choices:
        raise ValueError("LLM returned no choices")
    content = response.choices[0].message.content
    if content is None:
        return json.dumps({"action_type": "submit_solution", "sql": "SELECT 'no_content' AS error"})
    return content.strip()


def parse_action(text: str) -> dict:
    text = text.strip()
    if text.startswith("```"):
        lines = text.splitlines()
        inner = [l for l in lines if not l.startswith("```")]
        text = "\n".join(inner).strip()
    try:
        return json.loads(text)
    except json.JSONDecodeError:
        start = text.find("{")
        end = text.rfind("}") + 1
        if start != -1 and end > start:
            try:
                return json.loads(text[start:end])
            except json.JSONDecodeError:
                pass
    return {"action_type": "submit_solution", "sql": "SELECT 'parse_error' AS error"}


def env_post(path: str, **kwargs) -> dict:
    url = f"{ENV_BASE_URL}{path}"
    try:
        r = requests.post(url, timeout=30, **kwargs)
        r.raise_for_status()
        return r.json()
    except Exception as exc:
        print(f"  [env_post] Error at {path}: {exc}")
        raise


def env_get(path: str) -> dict:
    url = f"{ENV_BASE_URL}{path}"
    try:
        r = requests.get(url, timeout=30)
        r.raise_for_status()
        return r.json()
    except Exception as exc:
        print(f"  [env_get] Error at {path}: {exc}")
        raise


def run_episode(task_id: str) -> float:
    final_score = 0.0
    try:
        data = env_post("/reset", params={"task_id": task_id})
        session_id = data.get("session_id")
        obs = data.get("observation")
        if not session_id or not obs:
            print(f"  ERROR: Invalid reset response for {task_id}")
            return 0.0

        conversation: list = []

        for step_num in range(1, MAX_EPISODE_STEPS + 1):
            if obs.get("done"):
                break

            user_msg = (
                f"TASK:\n{obs.get('task_description', 'No description')}\n\n"
                f"Available tables: {obs.get('available_tables', [])}\n"
                f"Step: {obs.get('step_count', 0)} / 25\n"
                f"Last result: {json.dumps(obs.get('last_result'), default=str)[:600]}\n"
                f"Last error: {obs.get('last_error') or 'none'}\n"
                f"Recent history: {json.dumps(obs.get('query_history', [])[-3:])}\n\n"
                "What is your next action? (JSON only)"
            )

            conversation.append({"role": "user", "content": user_msg})

            try:
                llm_text = call_llm(conversation)
            except Exception as exc:
                print(f"  [step {step_num}] LLM error: {exc}")
                llm_text = json.dumps({"action_type": "submit_solution", "sql": "SELECT 1"})

            conversation.append({"role": "assistant", "content": llm_text})
            action = parse_action(llm_text)

            action_label = action.get("action_type", "?")
            action_detail = action.get("table_name") or (action.get("sql", "")[:60])
            print(f"  [step {step_num}] {action_label}: {action_detail!r}")

            try:
                step_data = env_post(f"/step/{session_id}", json=action)
            except Exception as exc:
                print(f"  [step {step_num}] Environment error: {exc}")
                break

            obs = step_data.get("observation", {})
            reward = step_data.get("reward", {})
            reward_val = reward.get("value", 0.0)
            reward_msg = reward.get("reason", "")[:70]
            print(f"           reward={reward_val:+.4f}  msg={reward_msg!r}")

            if step_data.get("done"):
                info = step_data.get("info") or {}
                final_score = info.get("score", 0.0)
                print(f"\n  ✓ Episode finished.  SCORE = {final_score:.4f}")
                if "grading_details" in info and isinstance(info["grading_details"], dict):
                    for k, v in info["grading_details"].items():
                        print(f"    {k}: {v}")
                break
    except Exception as exc:
        print(f"  ERROR: Episode crashed for {task_id}: {exc}")
        import traceback
        traceback.print_exc()

    return final_score


def main() -> None:
    print("=" * 60)
    print("  SQL Debug OpenEnv — Baseline Evaluation")
    print(f"  ENV  : {ENV_BASE_URL}")
    print(f"  MODEL: {MODEL_NAME}")
    print("=" * 60)

    try:
        health = env_get("/health")
        print(f"  Server health: {health}")
    except Exception as exc:
        print(f"ERROR: Cannot reach environment at {ENV_BASE_URL}\n  {exc}")
        sys.exit(1)

    tasks = ["fix_broken_query", "write_business_query", "complex_analytics"]
    scores: dict = {}
    t0 = time.time()

    for task_id in tasks:
        score = run_episode(task_id)
        scores[task_id] = score

    elapsed = time.time() - t0

    print("\n" + "=" * 60)
    print("  FINAL SCORES")
    print("=" * 60)
    for tid, s in scores.items():
        bar = "█" * int(s * 20) + "░" * (20 - int(s * 20))
        print(f"  {tid:<28} {s:.4f}  [{bar}]")
    avg = sum(scores.values()) / len(scores)
    print(f"\n  Average score: {avg:.4f}")
    print(f"  Total time:    {elapsed:.1f}s")
    print("=" * 60)

    with open("baseline_scores.json", "w") as fh:
        json.dump({"scores": scores, "average": avg, "elapsed_seconds": elapsed}, fh, indent=2)
    print("\n  Scores saved to baseline_scores.json")


if __name__ == "__main__":
    main()

──────────────────────────────────────────────
FILE: main.py  (only the /step endpoint needs fixing — return full file)
──────────────────────────────────────────────
[The full main.py content is in the repository at https://github.com/Dhakshin2007/my-openenv/blob/main/main.py]
Key section to fix — the /step endpoint leaderboard block:

    if result.done:
        score = result.info.get("score")
        if score is not None:
            task_id = env.state().task_id if not result.done else result.observation.task_id  # ← BUG HERE
            entry = {
                "score": score,
                "steps": result.observation.step_count,
                "session_id": session_id[:8],
            }
            board = _leaderboard[task_id]
            board.append(entry)
            board.sort(key=lambda x: (-x["score"], x["steps"]))
            _leaderboard[task_id] = board[:10]
        _sessions.pop(session_id, None)

──────────────────────────────────────────────
FILE: environment.py  (two bugs — return full file)
──────────────────────────────────────────────
[The full environment.py content is in the repository at https://github.com/Dhakshin2007/my-openenv/blob/main/environment.py]
Key sections to fix:
1. _trigger_chaos method — hardcoded INSERT
2. _execute method — sql.split(";") breaks CTEs

──────────────────────────────────────────────
FILE: Dockerfile
──────────────────────────────────────────────
FROM python:3.11-slim
RUN useradd -m -u 1000 user
WORKDIR /app
COPY --chown=user:user requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY --chown=user:user . .
USER user
EXPOSE 7860
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:7860/health')"
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "7860", "--workers", "1"]   # ← FIX THIS LINE

═══════════════════════════════════════════════════════════════
INSTRUCTIONS FOR YOUR RESPONSE:
═══════════════════════════════════════════════════════════════
1. Return the COMPLETE fixed content for each file: inference.py, main.py, environment.py, Dockerfile
2. Do NOT summarize or explain — just return the full corrected file contents
3. Clearly separate each file with a header like: === FILE: inference.py ===
4. Do not change any logic unrelated to the 5 bugs listed above
5. Preserve all existing comments, docstrings, and code structure
